services:
  guardrailed-agent:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY
      - GEMINI_API_KEY
      - GOOGLE_API_KEY
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
